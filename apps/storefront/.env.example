# Newshub storefront — local development (copy to .env.local, never commit env files)
# Public reading API base (API Contract v1.1, base path /api/v1).
# When unset, the storefront renders from the local static data layer.
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"
